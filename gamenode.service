# Simple systemd scenario for gamenode
# - put it in @HOME@/.config/systemd/user/
# - run: systemd --user reload-daemon
# - use: systemctl --user <status|start|stop> gamenode
###########################################################

[Unit]
Description = GameNode: interact with running games (node server)

[Service]
Type = simple
StandardOutput = journal
ExecStart = %h/.local/bin/gamenode -d9

[Install]
WantedBy=default.target

